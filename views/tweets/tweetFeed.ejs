<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Twitter Feed</title>
</head>

<body>
    <h1>HELLO FROM TWEET FEED!</h1>
    <!-- looping through the tweetFeed array and putting each tweet in a list -->
    <ul>
        <% for(let tweet of tweetFeed) { %>
            <li>
                <%= tweet.tweet %> -
                    <strong>
                        <%= tweet.user %> - <a href="/tweets/<%=tweet.id %>">Details</a>
                    </strong>
            </li>
            <% } %>
    </ul>
    <a href="tweets/new">Make a new tweet!</a>
</body>

</html>